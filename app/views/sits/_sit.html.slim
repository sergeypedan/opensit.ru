article.activity
  = link_to sit_path(sit), class: "title"
    => fa_icon (sit.is_meditation? ? "clock-o" : "book")
    = (sit.is_meditation? ? "#{sit.duration} minutes" : sit.title)

  = date_tag(sit.created_at)

  .body = teaser(sit, 90)
